[theme]
base                     = "light"
primaryColor             = "#3d5a1c"
backgroundColor          = "#f0f4e8"
secondaryBackgroundColor = "#ffffff"
textColor                = "#0e1308"
font                     = "sans serif"

[server]
headless         = true
enableCORS       = false

[browser]
gatherUsageStats = false
```

4. Click **Commit changes**

---

## requirements.txt

Make sure your `requirements.txt` has all needed packages. Replace it with:
```
streamlit
pandas
numpy
yfinance
plotly
requests
reportlab
